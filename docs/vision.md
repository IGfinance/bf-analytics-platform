# Куда мы движемся — видение платформы аналитики

Этот файл — ориентир для того, как отдельные ТЗ складываются в общую картину.
Перед тем как браться за новое ТЗ из `tz/`, полезно свериться, какую часть
этой картины оно закрывает.

## Что строим

Внутренний инструмент команды Finance Black (не отдельный продукт для
клиентов) — по каждому клиенту:
1. **Автоматически собирает** данные из разных источников (WB, Ozon, банк и
   т.д.) вместо ручного сведения в Excel.
2. **Проверяет** их между собой на расхождения — как внутри одного источника
   (сходятся ли отчёты одной площадки), так и между источниками (сходится ли
   отчёт площадки с банковской выпиской).
3. **Показывает** результат в дашбордах Metabase — и по каждому клиенту
   отдельно, и (для клиентов с несколькими кабинетами/брендами) сводно.

## Как это устроено технически (коротко)

- Каждый источник данных (WB, Ozon, банк, ...) — свой независимый модуль
  с одинаковым принципом: скачал/принял файл → разобрал → загрузил в
  ClickHouse. Новый источник = новый модуль по этому же принципу, не
  переделка существующих.
- У каждого клиента — своя база данных в ClickHouse (не общая таблица на
  всех) — чтобы данные разных клиентов физически не пересекались.
- Проверки расхождений — это конфигурируемые правила (как сейчас
  `reconciliation_rules_wb.yaml`), а не захардкоженная логика в коде —
  правил будет постепенно много, они копятся по мере того, как находятся
  новые способы что-то проверить.

## Прогресс: ТЗ → часть видения → статус

| ТЗ | Что закрывает | Статус |
|----|----|----|
| [01 — WB: сверка сводного отчёта](tz/done/01-Чел-260816-platform-wb-sverka-otcheta.md) | Первая проверка расхождений внутри одного источника (WB) | готово, см. [review](tz/review/01-260902-platform-wb-sverka-otcheta-review.md) |
| Ozon-коннектор | Второй источник данных по тому же принципу, что и WB | готово, см. коммит `7129ea3` (API-загрузка + сверка + `ozon_metrics_by_cabinet_month`). Не отдельное ТЗ Илье — сделано напрямую |
| [02 — Реальт: источники данных и метрики](tz/done/02-Чел-260912-realt-istochniki-metriki.md) | Первый клиент за пределами WB/Ozon-маркетплейсов — расширяет принцип «новый источник = новый модуль» на другой тип бизнеса (Клиентикс, Google-Таблицы), метрики (выручка по услугам, визиты, клиенты, ФОТ, расходы) в Metabase | готово, см. [review](tz/review/02-260915-realt-istochniki-metriki-review.md) |
| Разграничение проектов в ClickHouse (control + per-project БД) | «У каждого клиента — своя база данных» (строка выше) — из планов стало фактом: `control` (projects/users/user_projects) + `cloudsix` + `realt`, webapp резолвит БД по проекту динамически. Не отдельное ТЗ Илье — сделано напрямую, коммит `e94595e` | готово, разграничение прав (см. бэклог ниже) — нет |
| Реальт: источники «Расчетный счет»/«Наличные»/«Начисления» + метрики/дашборды в Metabase | Ещё 3 источника Реальта по тому же принципу (Google-Таблицы) + 67 Metric/7 Model/4 карточки/2 дашборда поверх них | готово, но в обход процесса (нет ТЗ, ветка с 3 источниками была смержена после того, как данные уже легли в прод) — найденные архитектурные долги (дублирование формул в native SQL карточек 117/177, 0 тестов на новые парсеры) разобраны 2026-09-17/19, тесты вынесены в ТЗ 03 |
| [03 — Реальт: тесты для финансовых источников](tz/active/03-Чел-260917-realt-testy-fin-istochnikov.md) | Юнит-тесты на `parse_bank_account`/`parse_cash`/`parse_accruals` — закрывает пробел из строки выше | в работе у Ильи |
| Реальт: клиентский доступ в Metabase | Изоляция прав `All Users` (было: write-доступ ко всему по умолчанию), отдельная коллекция «Реальт (дашборды)» + группа-вьювер для клиента | готово частично — аккаунт клиента заведён, но без рабочего пароля (SMTP на сервере не настроен, см. бэклог) |

## Бэклог (мелкие доработки, не тянут на отдельное ТЗ)

- **Канарейка на переименование статей Ozon.** `ozon_metrics_by_cabinet_month`
  (см. `src/schema_ozon_metrics_views.sql`) считает "Выручка к перечислению"
  через захардкоженный список названий `service_group`/`accrual_type` (по
  образцу того, как WB-модель уже обрабатывает переименование "Продвижение
  ВБ"→"Продвижение WB"). 2026-09-08 поймали живое переименование Ozon
  ("Услуги агентов"→"Услуги партнёров") только руками, сравнив с
  `sum(total_amount)`. Нужна проверка в `check_ozon.py` (по образцу уже
  существующих там проверок), которая раз в прогон сверяет
  `payable_total` из VIEW с `sum(total_amount)` по всем строкам за тот же
  месяц — расхождение = сигнал, что Ozon добавил/переименовал ещё одну
  группу услуг, которую формула не учитывает.

- ~~**Диск на VPS почти заполнен.**~~ Решено 2026-09-12: причиной было не
  само хранение данных (`cloudsix`+`control` вместе — 100MB), а служебные
  таблицы `system.*` самого ClickHouse (`trace_log`, `text_log`,
  `metric_log`, `part_log`, `asynchronous_metric_log`) без TTL — разрослись
  до 4.1GB. Поставлен TTL (7 дней, `part_log` — 14) через
  `ALTER TABLE ... MODIFY TTL`, старые данные вычищены. Плюс: `journalctl
  --vacuum-size=300M` + `SystemMaxUse=500M` в `journald.conf` (было 1.9GB),
  `apt-get clean` (267MB), `docker image prune` (0 — образы не оказались
  лишними), удалён неиспользуемый `/var/www/html/venv` (154MB, не тот, что
  использует `finwise.service` — тот на `venv2`, проверено через
  `systemctl`/`ps`). Диск: 97% → 67% (770MB → 6.2GB свободно). Осталось на
  будущее: алерт на заполнение диска, чтобы не ловить это руками в
  следующий раз.

- **WB API вообще не грузит данные.** `wb_api_realization` — 0 строк для
  CloudSix (обнаружено 2026-09-13 через `compare_wb_sources.py`, который
  ранее падал с крашем на пустой таблице и маскировал находку). `ingest_wb_api.py`
  в репозитории есть, но, похоже, ни разу не был прогнан (в отличие от
  Ozon, где `ozon_api_transactions` реально наполнена, см. `7129ea3`).
  **Не трогать 16 дней** — сейчас действует бан WB по API-запросам.
  После снятия — разобраться и прогнать.

- **Узкие ClickHouse-пользователи для Metabase.** Сейчас Metabase
  подключается к `cloudsix` под тем же широким пользователем `default`,
  что и сам webapp/CLI-скрипты (временно, после переноса CloudSix в
  `cloudsix` 2026-09-12) — GRANT-изоляция между проектами для аналитика
  ещё не настоящая, только БД разделены физически. Нужно завести
  `metabase_cloudsix`/`metabase_realt` с SELECT только на свою БД и
  переключить на них Metabase-подключения (см.
  `.claude/plans/iridescent-brewing-scroll.md` → «Кто куда подключается»).
  Плюс `scripts/provision_project.py` — пока Реальт заведён вручную,
  третьего клиента лучше заводить воспроизводимым скриптом.

- **SMTP для клиентских аккаунтов Metabase.** Аккаунт клиента Реальта
  (`violetta7799@gmail.com`) заведён 2026-09-19 без рабочего пароля — на
  сервере не настроена отправка почты, а без неё Metabase не может выслать
  приглашение/сброс пароля. Нужно решить (варианты обсуждены: Gmail
  app-password vs транзакционный сервис) и завести SMTP, прежде чем можно
  будет заводить следующих клиентских пользователей самообслуживанием.

- **Дублирование формул в native SQL карточек Metabase (Реальт).** Найдено
  2026-09-17/19: формулы разбивки ФОТ по `pay_type` продублированы в 3
  Model (98/119/120) вместо ClickHouse VIEW, 24-статейный whitelist
  P&L-расходов — в Model 178 и карточке 179, карточки 117/177 — третья
  независимая реализация той же логики с отдельным (осознанно другим)
  ключом исключения ШАА. Первые два случая уже переведены на тонкие
  обёртки над VIEW; карточки 117/177 намеренно не тронуты — у них другая
  семантика тумблера «Учитывать Шмиловича» (исключает весь `department`,
  а не только ФОТ по роли), наивный перевод на общую VIEW сдвинул бы цифры
  при выключенном тумблере. Нужна отдельная проработка, не мелкая правка.

## Changelog

- 2026-08-24: создано
- 2026-09-08: добавлен Ozon-коннектор через API (см. таблицу ниже), начат семантический VIEW-слой для Ozon (`ozon_metrics_by_cabinet_month`)
- 2026-09-12: поставлено ТЗ 02 — источники данных и метрики Реальта (второй клиент, первый за пределами WB/Ozon)
- 2026-09-12: разграничение проектов в ClickHouse (control + cloudsix + realt) выполнено; добавлены в бэклог — диск VPS на 97%, узкие ClickHouse-пользователи для Metabase
- 2026-09-12: поправлена таблица прогресса — строка «Ozon-коннектор» помечена готовой (была ошибочно «не начато» после коммита `7129ea3` от 2026-09-08); у ТЗ 02 отмечено снятие блокера (БД `realt` заведена)
- 2026-09-12: разобрались с диском VPS (97%→67%) — виноваты были `system.*` таблицы ClickHouse без TTL, не миграция; пункт бэклога закрыт, осталось только завести алерт на будущее
- 2026-09-13: сверка Ozon расширена с «общая сумма» до всех 12 метрик модели (`ozon_metrics_by_cabinet_month_api`, `compare_ozon_metrics.py`, Metabase-карточка 65); попутно поправлена классификация сторно доставки покупателю (в «Возвраты», не в «Продажи») — и в API-VIEW, и в боевой `ozon_metrics_by_cabinet_month`
- 2026-09-13: починен двойной счёт возвратов в WB loyalty-метриках (`loyalty_program_cost`, `loyalty_points_deducted`) — нашли не только в `reconciliation_rules_wb.yaml`, но и в боевой `wb_metrics_by_cabinet_month` (Metabase Model 49): `payable_total` был занижен на 1.4–3.3 тыс ₽/месяц за апрель-июль. Допуски всех сверок (Ozon и WB) ужесточены до <1₽. См. вики: [[2026-09-13 сверка Ozon по метрикам и двойной счёт возвратов в WB loyalty]]
- 2026-09-13: найдено — WB API не грузит данные (`wb_api_realization` пусто), добавлено в бэклог, не трогаем 16 дней (бан WB по API)
- 2026-09-14–15: полный отчёт WB по SKU + сверки WB по образцу Ozon; Реальт — источники Клиентикс (.csv) и ФОТ (Google Sheets API), семантический слой метрик (`realt_metrics_by_month`), Metabase Model/дашборды, банк/карт-выписки Реальта через веб-форму — ТЗ 02 принято (см. review)
- 2026-09-17: Реальт — источники «Расчетный счет»/«Наличные»/«Начисления» (Google-Таблицы) + 67 Metric/7 Model/4 карточки/2 дашборда в Metabase, сделано Ильёй в обход процесса (нет ТЗ); при разборе найдены дублирование формул в native SQL карточек и 0 тестов на новые парсеры — тесты вынесены в ТЗ 03, дублирование частично устранено (см. бэклог)
- 2026-09-19: Реальт — изоляция прав Metabase для клиента (была: `All Users` = write-доступ ко всему), новая коллекция + группа-вьювер, аккаунт клиента заведён (без пароля, см. бэклог SMTP); починен баг с занижением выручки за январь-март 2026 из-за ключа исключения ШАА без учёта периода (Онегина Е.Ю. сменила отдел в течение стажа); задокументировано осознанное исключение из правила «на дашборде только Визуалы» для сводных Таблиц с ratio-метриками (`.claude/knowledge/architecture-standarts.md`, `docs/architecture-map.md`, `docs/formulas/realt.tex`)
- 2026-09-19: README.md и docs/vision.md/glossary.md приведены в соответствие текущему коду (Ozon/банк/карты/ПланФакт/Клиентикс/Реальт вместо только WB) — `docs/architecture-map.md` оставлен как снимок на 2026-09-13, следующее обновление отдельной задачей (нужен live-снимок ClickHouse/Metabase)
- 2026-09-19: Реальт — накладные расходы (ФОТ Админов+Управления+аренда/коммуналка/остальной P&L, поровну на визиты клиники) и маркетинг (ФОТ Маркетинг+реклама+подрядчики+телефония, 80%/20% психиатрия/психология по решению владельца) добавлены в юнит-экономику по врачу — карточки 183/184 дашборда id 7 (новые строки/колонки + «Прибыль после всех затрат»); карточки обновлены напрямую в Metabase через API (токен `METABASE_API` из `.env`) и проверены сквозным выполнением через сам Metabase (не только прямым SQL); по пути найдены и обойдены два ГОЧТЯ — лимит планировщика ClickHouse на 26-way UNION ALL (`SETTINGS query_plan_max_optimizations_to_apply`) и повторное срабатывание давнего бага «параметр в комментарии ломает JDBC-парсер» (внёс сам, убрал `{{...}}` из своих же комментариев)
- 2026-09-19: попутно найден и починен независимый старый баг — строки «ФОТ Администраторы/Управление/Маркетинг» карточки 183 были пустые при фильтре «Тип врача» ≠ «Все» (условие фильтра не пропускало эти role_group ни в одну ветку); почин имел побочный эффект — «ФОТ всего»/«Прибыль после ФОТ» под тем же фильтром стали делить клиники-целиком накладной ФОТ на визиты одного типа врача (искусственно занижало прибыль на порядок), исправлено вторым шагом — fot_total/taxes_total по-прежнему считаются только по клиническому ФОТ выбранного типа, см. `docs/formulas/realt.tex` раздел 5.4.1
- 2026-09-19: построчная сверка накладных с реальным P&L владельца (Google Sheets) вскрыла три самостоятельных источника расхождения — 1) двойной счёт в моей же формуле «Прибыль после ФОТ и накладных» (fot_total уже включал Админов/Управление при doctor_type=«Все», плюс их же включали накладные — занижало прибыль на ~2750 ₽/визит, исправлено новой парой колонок fot_own/taxes_own); 2) АПДШ (отдельное юрлицо)/«Шмилович личное» утекали в статьи P&L наравне с клиникой (~2.9 млн ₽/год), и «Расчётный счёт» дублировал часть сумм с «Начислениями» (один платёж считался целиком в Р/с и ещё раз размазанным по месяцам в Начислениях) — оба фикса в `realt_pl_by_group_month` (общий VIEW, чинит заодно и старую карточку 179 «П&Л по группам»), после фикса 104 из 112 сверенных ячеек (статьи×месяцы) совпали день в день; 3) методика была в корне неверной — P&L владельца построен по этажам (2 этаж/3 этаж/ШАА), а не по роли визита, у каждого этажа свои администраторы и своя аренда — переписано на per-этаж атрибуцию (новая колонка `visit_floor` в `realt_visits_categorized`, из department в ФОТ), Управление+мелкая административка+кредиты/амортизация делятся только между 2 и 3 этажом пропорционально их выручке (ШАА не участвует), налог на прибыль временно исключён из накладных (владелец определит отдельной формулой). Итог: «Прибыль после ФОТ и накладных» стала в основном положительной (было стабильно отрицательной −2800…−5500 ₽/визит). Открытый вопрос: выручка 2/3 этажа расходится с таблицей владельца на ~266 тыс ₽/мес (3,3%, зеркально), причина не найдена — ни один врач не меняет department между этажами за год. Подробности и все формулы — `docs/formulas/realt.tex` раздел 5.4 и раздел про P&L (5.4-fixes).
- 2026-09-24: Реальт — новый дашборд «Визиты за 3 мес по врачам» (id 12, карточки 196 «метрика» и 197 «знаменатель»): визиты нового клиента К СВОЕМУ ЖЕ врачу за скользящее окно [месяц привлечения .. +2 мес.], строки — ФИО врачей по типам (Психиатры/Психологи/Прочие), столбцы — месяц привлечения; ШАА без тумблера, обычными строками. Тип врача берётся из `realt_payroll.role` по последнему периоду, а НЕ из `role_group` визита (тот зависит от наличия ФОТ в месяце визита и до 2025-01 равен «Без ФОТ-кода» у всех). Попутно найден баг в боевой VIEW: в `rolling_m` (`realt_metrics_by_month.new_client_visits_3m`, строка карточки 186 «Визиты на нового клиента (3 мес, скользящее)») алиас `acquisition_month AS month` затеняет колонку `month` внутри `countIf`, условие окна вырождается в тождество — VIEW считает ВСЕ визиты когорты за всю историю, а не за 3 месяца (январь 2026: 567 вместо 436, завышение 20–30% во всех месяцах кроме 1–2 последних, где сходится случайно). Фикс не применён — ждёт решения владельца, меняет уже просмотренные им числа. Вторая ГОЧТЯ: `realt_visits_categorized.visit_seq` недетерминирован (row_number без tie-break, у 98 из 8072 клиентов первый визит — несколько визитов с одинаковым `visit_start` к разным врачам), опираться на него при определении «врача первого визита» нельзя — в карточке 196 детерминированный `argMin(..., (visit_start, doctor_name))`. Обе ГОЧТЯ и формулы — `docs/formulas/realt.tex`, разделы «Визиты новых клиентов за скользящий квартал» и «Визиты за 3 мес по врачам» (realt.pdf НЕ перегенерирован — на машине нет xelatex/tectonic)
- 2026-09-24 (тем же днём, по решению владельца): баг окна в `rolling_m` ПОЧИНЕН на проде — алиас переименован в `cohort_month`, в финальном SELECT `r.month` → `r.cohort_month` (сам `countIf` не менялся). Строка «Визиты на нового клиента (3 мес, скользящее)» карточки 186: январь 2026 2.81→2.16, февраль 2.29→1.88, март 2.08→1.74, июнь/август без изменений, «За год» 2.24→1.80; знаменатель (`new_clients`) не затронут. Сверено, что единственное изменение в развёрнутой VIEW — переименование алиаса (диff `create_table_query` до/после). Попутно вскрылась ещё одна ГОЧТЯ: `CREATE OR REPLACE VIEW` СБРАСЫВАЕТ все `COMMENT COLUMN` — в проде на `realt_metrics_by_month` осталось 4 комментария из 37, восстановлены все 37. DDL накатывался через `/api/dataset` Metabase (SSH-туннель к ClickHouse не нужен): JDBC выполняет statement, Metabase отдаёт HTTP 400 «SELECT не вернул ResultSet» — это не ошибка выполнения, проверено на probe-VIEW
