# Журнал задач — Илья

Короткие записи после каждой проверки через `/review-task`. Новые записи —
в конец файла.

## 2026-08-23 — WB: кабинет выпадающим списком + сверка сводного отчёта
- ТЗ: work/tz/active/01-Чел-260816-platform-wb-sverka-otcheta.md (+ `01-Тех-...`)
- Сделано: даталист кабинетов на обеих формах, загрузка сводного отчёта
  (`wb_summary_core.py` → `wb_report_summary`), сверка с сырыми данными
  (`reconcile_wb.py`, конфиг формул `reconciliation_rules_wb.yaml`,
  формулы подтверждены на 6 парах реальных отчётов), дашборд в Metabase
- Найденные баги/проблемы: SQL-инъекция через `cabinet` в `reconcile_wb.py`
  (f-string вместо параметризованных запросов, как в `check_wb.py`) —
  блокирует приёмку; непромапленные колонки сводного отчёта логируются
  только в HTTP-ответе, без постоянного лога (в отличие от
  `wb_unmapped_columns_log`); пропала строка `*.log` в `.gitignore`
- Обновления architecture-standarts.md: добавлена SQL-инъекция через f-string
  в чёрный список антипаттернов + golden example (check_wb.py — правильный
  параметризованный паттерн — vs reconcile_wb.py — было)
- Изменения в скилах: `review-task/SKILL.md` дополнен обязательным шагом
  локальной загрузки файлов подрядчика (`git show origin/branch:path`) +
  py_compile/импорт + живой PoC для находок безопасности, без обращения к
  проду без подтверждения пользователя

## 2026-09-02 — WB: фикс формулы «Компенсация скидки по программе лояльности»
- ТЗ: work/tz/done/01-Чел-260816-platform-wb-sverka-otcheta.md (+ `01-Тех-...`)
- Сделано: `reconciliation_rules_wb.yaml` — формула заменена с `sum()` на
  `sumIf(Продажа) − sumIf(Возврат)`, допуск снижен с 1500 до 0.01 (старая
  формула задваивала возвраты). Коммит Ильи `85dc007` на `origin/draft`
  (только этот коммит с этой ветки — остальные ~15 коммитов той же ветки
  оказались другого автора, MMElena23, несвязанная фича «Доходы и расходы»
  без ТЗ, по решению пользователя не рассматривались)
- Найденные баги/проблемы: нет новых. Блокирующая SQL-инъекция из
  прошлого ревью — подтверждено, уже устранена в `master` независимо.
  Живой PoC на прод-данных (CloudSix, report_number 595018780 и 608445079)
  подтвердил точное совпадение новой формулы со сводным отчётом там, где
  старая расходилась на 36–102 руб.
- Обновления architecture-standarts.md: нет
- Изменения в скилах: нет

## 2026-09-15 — Реальт: источники данных и метрики
- ТЗ: work/tz/done/02-Чел-260912-realt-istochniki-metriki.md (+ `02-Тех-...`)
- Сделано: 5 форм загрузки (банк 1С, карты PDF, Клиентикс CSV, Google-Таблицы
  «Зарплаты»/«Расходы по статьям» — pull через Sheets API), `project_sources`
  управляет видимостью вкладок без хардкода, `bank_statement_1c`/
  `card_statement_pdf` рефакторены в переиспользуемые `ingest_files`. Три
  ClickHouse VIEW (`realt_metrics_by_month`, `realt_revenue_by_service`,
  `realt_expenses_by_month`) с `COMMENT COLUMN`, три тонкие Metabase Model
  в коллекции «Реальт» (id 8, проверено API). Self-check: сверка с черновиком
  дашборда клиента — 54/55 месяцев точное совпадение выручки/визитов/
  клиентов (расхождение объяснено и в нашу пользу), ручной чек-лист по
  каждому источнику
- Найденные баги/проблемы: нет блокирующих. Один процессный нюанс — 9 из 10
  коммитов ушли напрямую в master без PR (ТЗ просило «одна ветка/один PR»),
  код не пострадал, отмечено для Ильи на будущее
- Обновления architecture-standarts.md: нет (правило клиент+площадка в
  имени Метрики/Визуала добавлено раньше в этот же день, отдельной сессией
  по Metabase-таксономии — на Model, которые завёл Илья, не распространяется)
- Изменения в скилах: нет
